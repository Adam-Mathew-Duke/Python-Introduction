# dictionaries

# keys and values
# key = 'Bob'
# value = 12
# do not use any duplicate keys
# keys must be unique
students = {'Bob':12,'Rachel':13,'Emily':15}

# update a keys value
students['Rachel']=14

# call a key to get it's value
print (students['Rachel'])

# remove a value
del students['Emily']
print (students)

# number of entries in the dictionary
print (len(students))
